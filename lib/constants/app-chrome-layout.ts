/**
 * 홈(`page.module.css`)·리포트(`report.module.css`) 콘텐츠 max-width 와 맞출 때 참고.
 * 햄버거·사이드 패널은 뷰포트 우측 고정이라 이 값으로 배치하지 않음.
 */
export const APP_CHROME_INNER_MAX_HOME = 960 as const
export const APP_CHROME_INNER_MAX_REPORT = 1200 as const
